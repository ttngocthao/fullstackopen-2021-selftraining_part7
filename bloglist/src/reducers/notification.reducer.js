const initialState = {
  visible: false,
  message: '',
  successful: true
}

const notificationReducer = (state=initialState,action) => {
  switch(action.type){
  default:
    return state
  }
}

export default notificationReducer